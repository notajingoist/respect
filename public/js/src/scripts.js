var SITE = {
    init: function() {
        this.setVars();
        this.bindEvents();
    },

    setVars: function() {
        this.$window = $(window);
    },

    bindEvents: function() {

    }
};

SITE.init();
